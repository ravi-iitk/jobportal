export default function Skeleton({ lines = 3 }) { return <div className="space-y-3">{Array.from({ length: lines }).map((_, i) => <div key={i} className="h-5 animate-pulse rounded bg-slate-200 dark:bg-slate-800" />)}</div>; }
